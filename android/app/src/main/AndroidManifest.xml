<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vaktinamaz.app">

    <!-- 🌐 İnternet izni (Firebase, API, reklamlar için) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📍 Konum izni (Kıble, namaz vakti konum belirleme için) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 🧭 Cihazda pusula sensörü varsa kullanılsın -->
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false"/>

    <!-- 👣 Adım sensörü (Step Counter) varsa kullanılsın -->
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false"/>
    <uses-feature android:name="android.hardware.sensor.stepdetector" android:required="false"/>

    <!-- 🟢 Arka planda çalışan servisler için zorunlu izin -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- 🟡 Android 10+ için aktivite/step sensor izni (adım sayar için 100% zorunlu) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <!-- 🔋 Arka planda sensör okumaları kesilmesin diye (bazı cihazlarda gerekli) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.vaktinamaz.app.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:label="@string/title_activity_main"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔥 Adım Sayar Servisi (arka planda sürekli çalışır) -->
        <service
            android:name=".StepService"
            android:exported="false"
            android:enabled="true"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
